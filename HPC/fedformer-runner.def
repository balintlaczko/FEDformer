Bootstrap: docker
From: pytorch/pytorch:2.1.0-cuda12.1-cudnn8-devel

# Apptainer definition file to run (train) the FEDformer model on HPC.

%post
    apt-get update
    pip3 install absl-py einops h5py keopscore opt-einsum pandas pytorch-wavelet PyWavelets scikit-image scikit-learn scipy statsmodels sympy tqdm